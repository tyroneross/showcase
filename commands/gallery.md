---
name: gallery
description: Overview of all captures grouped by feature, date, component, or platform
allowed-tools: ["showcase"]
---

Show the Showcase gallery using the `gallery` tool.

Default grouping is by feature. If the user asks for a different grouping (by date, component, or platform), pass the appropriate `group_by` parameter.

---
name: export
description: Export captures to an output directory with markdown manifest
allowed-tools: ["showcase"]
---

Export captures for blog/website use with the Showcase `export` tool.

If the user specifies which captures (by IDs, feature, tags, or starred), use those filters. Otherwise, export all starred captures. Default output is `./showcase-export` with a markdown manifest.
